# Change History (Alias)

This file is a quick release-log entry point.

- Canonical release notes: `docs/change-summary.md`
- Keep this file updated in each deploy cycle with a short pointer to the newest entry.

## Latest

- 2026-03-04: Added OCI parity verification and OCI-backed table CRUD seam (`EFL_DATA_SOURCE=oci`) to accelerate clean cutover while preserving existing route interfaces. See `docs/change-summary.md`.
